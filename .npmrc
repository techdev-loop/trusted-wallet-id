# Solana wallet adapters pull optional react-native/mobile peers; this web app uses React 18.
# Keeps npm install / Vercel from failing on peer conflicts with react-native@0.84 → react@19.
legacy-peer-deps=true
